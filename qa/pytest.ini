[pytest]
markers =
    smoke: Critical path tests
    navigation: Navigation link tests
    content: Content verification tests
    responsive: Mobile/responsive tests
    performance: Load time and performance tests
    accessibility: Accessibility and semantic HTML tests
    deployment: CI-only deployment verification tests
addopts = -v --tb=short
testpaths = tests
